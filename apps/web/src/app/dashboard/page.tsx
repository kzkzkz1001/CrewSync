'use client';

// TODO: implement the manager dashboard
// - Shift list + create shift form
// - Live map with vehicle position (socket.io-client → WS_EVENTS.GPS_UPDATE)
// - Pickup node markers

export default function DashboardPage() {
  return (
    <div>
      <h1>Dashboard</h1>
      <p>TODO: shift management + live map</p>
    </div>
  );
}
